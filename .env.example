# ─── Supabase ───
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# ─── Cron Secret (for auto-followup) ───
CRON_SECRET=your-cron-secret

# ─── Integration keys (configured in CRM Settings UI) ───
# Interakt WhatsApp API Key
# Bolna AI Agent ID + API Key
# Google Ads Webhook Key
# Meta App ID + Access Token + Pixel ID
# Portal API URL + Token (app.goeduabroad.com)
# Gmail App Password / Resend API Key
# Gemini API Key
